# File: default.env

# ==========================================================
# Global Application Settings
# These settings apply to both Go (Collector) and Python (Analyzer) services
# ==========================================================
WIZARD_ENV=local
WIZARD_LOG_LEVEL=DEBUG

# Define internal ports for local development
COLLECTOR_PORT=8080
ANALYZER_PORT=8000
API_PUBLIC_PORT=9000  # The port exposed by docker-compose for the Python API

# ==========================================================
# Database Configuration (for Python Analyzer service)
# These variables are consumed by the Python service locally
# ==========================================================
DB_HOST=db_local
DB_PORT=5432
DB_NAME=wizard_db_dev
DB_USER=wizard_user
# WARNING: NEVER use production secrets here. Use local, non-sensitive passwords.
DB_PASSWORD=local_dev_password

# ==========================================================
# External Service Mocks (for Go Collector service)
# ==========================================================
# In local development, the Go collector often sends data to a local Kafka or mock endpoint
KAFKA_BROKERS=localhost:9092
KAFKA_INPUT_TOPIC=raw_events_dev
KAFKA_OUTPUT_TOPIC=processed_events_dev

# ==========================================================
# Security & Tokens (Minimal local setup)
# ==========================================================
# A simple key for local testing only
WIZARD_API_KEY=dev_secret_key_12345